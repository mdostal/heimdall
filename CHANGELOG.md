# Changelog

## [Unreleased]

### Fixed

- **Operator's personal Tailscale IP was hardcoded as a live shipped default in telemetry code — a real privacy exposure for anyone who installed the public npm package.** Found by an independent code review immediately after `v0.33.0` went public as `pantheon-heimdall` on npm — the operator's own Tailscale IP (`100.75.161.82`), machine hostname (`thes-mac-studio.lan`), and Unix username (`dostal`) had shipped as hardcoded defaults and doc/example content, not just internal notes. Worst of it: `src/adapters/argus.ts`'s `DEFAULT_ARGUS_STATS_BASE_URL` and `src/core/telemetry/argus-client.ts`'s `DEFAULT_ARGUS_HOST` both defaulted to that real IP — any installer who didn't explicitly configure `ARGUS_STATS_URL`/`ARGUS_OTLP_HOST` would have their local telemetry silently attempt to reach the operator's own machine on every run.
  - Both defaults now resolve to `"localhost"` instead — Argus is normally co-located with Heimdall in a typical Pantheon install, so `localhost` is the only default that's safe for every operator's deployment, matching the reasoning `multica-rest-client.ts` already used for Multica. A remote/Tailscale Argus host stays available as a per-operator override, never a code-baked default.
  - Multica's own client was already correctly gated — `MULTICA_BASE_URL` was already required config with no baked-in default, so `multica-rest-client.ts` had no live exposure. Its comments/error text still named the real IP as an example, though, so that got genericized to `<your-tailscale-ip>`, along with the equivalent example lines in `.env.example`; `multica-rest-client.test.ts`'s real-IP fixture was swapped for an RFC 5737 documentation-range IP.
  - Same genericization pass across `docs/decisions/DEC-hdl-{local-build-verification,429-corroboration,role-actuation,scheduler-backend}.md` (real IP/hostname/username/paths replaced with placeholders, narratives preserved), and `scripts/hive-verify.sh`'s build-box host is now an overridable `HIVE_BUILD_BOX` env var (still defaults to the real value — it's a genuine operational script for the operator's own build box, not a public template).
  - Same review pass also caught: a stale Node `>=22.5.0` requirement in `README.md`/`CONTRIBUTING.md`/`DEC-hdl-local-build-verification.md` corrected to `>=22.13.0` (matching `package.json`'s actual `engines` field and `scripts/install.sh`); `package.json` was missing `license`/`repository`/`homepage`/`bugs` fields expected of a published npm package; `docs/vision.md` still described a pre-CLI, pre-install-story Heimdall despite `hdl-agent-onboarding` having shipped in `0.33.0`, now updated; and `README.md` carried a dead-linked "OSS Ready" badge and a "Build Status" badge pointing at a workflow that doesn't run on PRs, both removed and replaced with honest text pointing at the real local build-verification gate.
  - Version bump `0.33.0` → `0.33.1`.

## [0.33.0] - 2026-08-19

13 epics accumulated in `[Unreleased]` since `v0.20.0` (2026-08-14): route-outcome-feedback, own-telemetry, 429-corroboration, error-taxonomy, docs-viewer, desktop-app, routing-policy-dashboard, policy-path-fix, override-reason, unified-dashboard, desktop-icon-settings, icon-reconciliation, and agent-onboarding. Each entry below documents its own version bump inline.

### Added

- **`hdl-agent-onboarding` epic complete — Heimdall gets a real install story for the first time.** Operator: *"every agent, harness, etc that uses this needs a way to install and interact with it -- it should be top and forward so people can see when installing and working and what to feed the agent or harness to get interactivity,"* modeled on (not copied from) Portunus's own real, shipped `agent init`/`agent status` pattern. Heimdall started this epic with zero `bin` entrypoint, no packaged CLI, no MCP-registration story, zero skills, and a dashboard with no onboarding surface at all.
  - **Foundational bug fixed first, because it gated everything else**: every Heimdall entrypoint (`src/main.ts`, `src/api/http-server.ts`, `src/api/mcp-server.ts`, and `src/api/cli.ts`) defaulted to `new StateStore(process.env.HEIMDALL_DB_PATH ?? ":memory:")`. Fine for dev, where every process shares one `.env` — but a globally npm-installed `heimdall mcp`, spawned by an agent harness with no `.env` in scope, silently handed the agent an empty, ephemeral, disconnected database, not the real fleet. New `resolveDefaultDbPath(env, homedir)` (same shape as the existing `resolveDefaultPolicyPath`) resolves a fixed XDG-style default (`~/.local/share/heimdall/heimdall.db`) when `HEIMDALL_DB_PATH` isn't set, wired into all four construction sites, with SQLite WAL mode enabled for safe multi-process access. Live-verified with a real cross-process concurrent-access test — two separate local processes against the same resolved path with no `.env` present, a write through one confirmed visible through the other.
  - **Real packaged npm CLI**: Heimdall is now `pantheon-heimdall`, installed command `heimdall`, with an actual `bin` entrypoint — `"private": true` and pure `tsx`-against-`src/*.ts` dev-only invocation are gone. `heimdall agent init [--harness claude|codex]` and `heimdall agent status [--harness ...] [--json]` extend `src/api/cli.ts`'s existing dispatcher (new `src/cli/agent-command.ts`) rather than inventing a second one. Both detect and register Claude Code / Codex MCP servers, mirroring Portunus's real `agent_setup.py` shape — `detectHarnesses()` via `which`, `registerMcp()` shelling to `claude mcp add --scope user heimdall -- heimdall mcp` / `codex mcp add heimdall -- heimdall mcp`, and the targeted `claude mcp get heimdall` lookup (not `claude mcp list`, which Portunus already found health-checks every registered server and takes 30+s) for a fast, idempotent status check. Live-verified against this machine's real `claude`/`codex` binaries — Heimdall's own MCP server is currently registered.
  - **4 real usage skills** installed idempotently (content-diffed, not blind-overwrite) to `~/.claude/skills/`.
  - **`scripts/install.sh`** — a real `curl | bash` installer (`npm install -g` + `agent init`) — and npm publish-readiness (not the actual publish).
  - **The repo went public and GitHub Pages went live** at `https://mdostal.github.io/heimdall/`, fixing a pre-existing dead link the README had advertised since before this epic. Deployed branch-based (`build_type: legacy`, a `gh-pages` branch pushed to directly), *not* Actions-based — the originally-planned `actions/deploy-pages` workflow could never have run at all: GitHub Actions is account-wide billing-blocked for this repo right now, per `docs/decisions/DEC-hdl-local-build-verification.md`, discovered mid-planning rather than mid-execution.
  - New dashboard "get started" panel — prominent and dismissible, persisted server-side through the same `settings`-table pattern the theme/desktop-icon preferences already use, not `localStorage`.
  - **Real corrections an adversarial `grill` pass and a collaborative-review pass caught before any code was written**: the in-memory-DB gap above was originally scoped as 3 sites, not 4 — `src/api/cli.ts:57` was only found in the collaborative-review pass, after `grill` had already caught the other three. `grill` also caught a false claim in the first design-discussion draft that no CLI dispatcher existed at all (`src/api/cli.ts` already argv-dispatches `route`/`route-outcome`/default `lanes` subcommands via `route-command.ts`) — corrected so `agent init`/`status` extends that router instead of duplicating it. And the collaborative-review pass surfaced the GitHub Actions billing-block discovery above, which would have silently broken the original Actions-based Pages plan if it had gone straight to execution.
  - See `.pHive/epics/hdl-agent-onboarding/docs/design-discussion.md`. Version bump `0.32.0` → `0.33.0`.

- **`hdl-desktop-icon-settings` epic complete — the desktop app's tray icon now reflects the operator's Settings-panel choice.** Follow-up to `hdl-unified-dashboard`, which shipped the Node-side icon preference persistence; this completes the Rust side. Rasterized all 3 icon concepts (Watchtower, Routing Mark, Signal Horn) from the earlier UI/icon-pass mockups to real PNGs and generated full macOS icon sets via `cargo tauri icon` — Watchtower (the operator's chosen default) replaces the crude placeholder icon that had been in place since `hdl-desktop-app`.
  - **Real composition bug found and fixed**: the Signal Horn concept was badly off-center (bounding box crammed bottom-right, outer signal arc clipped off the canvas edge entirely) — confirmed by measuring actual rendered pixels (Playwright's screenshot tool doesn't work in this environment, so canvas data was extracted via `toDataURL()` and decoded directly), not by reading the drawing code. Fixed as a pure positioning change; independently re-verified with a fresh pixel measurement.
  - Rust wiring: the tray gets an explicit id so it can be looked up later (`app.tray_by_id` — there's no default-lookup shortcut), `sidecar.rs` fetches the preference from the sidecar's own `/desktop-icon` and resolves the bundled icon path, `lib.rs` applies it once the sidecar is healthy via `tauri::image::Image::from_path` + `TrayIcon::set_icon` — simpler than initially planned once research found Tauri's own `set_icon` takes an `Image` directly, no separate `tray_icon` crate dependency needed.
  - Corrected the Settings panel's copy after realizing the first draft implied a fuller live update than the implementation delivers: the tray icon only updates on the *next* launch (the preference is read once at startup, not polled), and the Dock icon still needs an actual rebuild — a real, confirmed macOS/Tauri v2 platform limit, not a gap in this implementation.
  - Live-verified via `cargo tauri dev`'s real stdout: fetch → resolve → load → `set_icon` all succeeded end to end against the real running app.
  - See `.pHive/epics/hdl-desktop-icon-settings/docs/design-discussion.md`. Version bump `0.30.0` → `0.31.0`.

- **`hdl-unified-dashboard` epic complete — 3-theme dashboard, Fleet Scope radar, Model Catalog tree, Settings page.** Operator, after reviewing the 3 independent UI-redesign mockups side by side: *"the lane control is my favorite with the moving radar and the dots... The model catalog with the tree underneath is clearer and cleaner... most of the rest of it actually is the same, so we should be able to just swap the theming and styles and offer that in a settings page and that'd be REALLY SLICK! Let's roll with a unified feature view like that and settings swaps."*
  - Mission Control's Fleet Scope radar+dots and Terminal's tree-connector Model Catalog become permanent, universal widgets across every theme — not exclusive to their origin mockup. The radar was ported by back-computing the mockup's own real blip geometry (angle = index/N × 360° clockwise from top, radius by severity tier), not re-imagined.
  - New CSS custom-property token system — 3 named palettes (`mission-control` default, `harbor-watch`, `terminal`), each ported from its origin mockup's real token values — replaces every hardcoded color across the whole dashboard. Every existing panel inherits theming automatically since they already used the shared CSS classes now driven by tokens.
  - New `GET`/`POST /theme` (persisted, mirrors `/routing-strategy`'s pattern exactly) and a Settings panel with a live theme picker — applies instantly client-side, and the server sets `data-theme` on the initial HTML response so the correct theme paints on first load, never a flash of the wrong one.
  - Desktop-icon preference (`GET`/`POST /desktop-icon`) also wired here on the persistence side, sharing the Settings panel — real Rust-side tray-icon consumption is a separate follow-up, since Tauri v2 on macOS has a confirmed platform limit (no runtime Dock-icon swap, tray-icon only). The Settings panel is honest about this rather than implying a full live swap.
  - **Real bug found during live verification**: the 10-column lanes table could overflow the viewport at real browser widths, with no scroll container. Fixed with `overflow-x: auto` scoped to the table's own container — the page body itself never scrolls sideways.
  - See `.pHive/epics/hdl-unified-dashboard/docs/design-discussion.md`. Version bump `0.29.0` → `0.30.0`.

- **`hdl-override-reason` epic complete.** Operator, reviewing the UI-redesign mockups: *"we need the override toggle with an optional description to get added for why."* New `override_reason` column on `lanes`, mirroring the existing `manual_override`/`manual_reset_at` pair exactly. `setManualOverride`/`setLaneOverride` gain an optional `reason` param, threaded through `GET /lanes`, `POST /lanes/:laneId/override` (HTTP), `heimdall.lanes.override` (MCP), and a new reason input + display in the dashboard's override controls. Forced to `null` whenever the override itself clears back to "auto" — a reason with no active override would be stale leftover text. Live-verified in a real browser: typed a reason, clicked Disable, confirmed it persisted through `GET /lanes` and rendered in the DOM; found and fixed a real CSS class collision along the way (the new reason display accidentally shared `.reason` with two unrelated existing elements, making a scoped test query return the wrong element). See `.pHive/epics/hdl-override-reason/docs/design-discussion.md`. Version bump `0.28.0` → `0.29.0`.

### Fixed

- **`hdl-icon-reconciliation`: the Settings icon picker showed generic emoji, not the actual icon artwork.** Operator: "Use the actual icons we saw." First investigation pass chased the wrong problem (a real-looking but non-issue "legibility bug" in the Watchtower artwork, which the operator directly confirmed looks fine at every size) before being redirected to the actual bug: the icon picker built in `hdl-unified-dashboard` rendered each option as a placeholder emoji (🗼🔀📯) rather than the real designed artwork. New `GET /desktop-icon/:name/thumbnail.png` (validated against the existing `DESKTOP_ICONS` allowlist) serves the real 128×128 PNG; a new `HEIMDALL_ICON_SETS_ROOT` env var wired from the Rust sidecar mirrors the existing `HEIMDALL_REPO_ROOT` pattern exactly, since the icon-sets directory is a sibling Tauri resource, not nested inside the Node app's own bundled resource dir. Live-verified: curled the route directly (valid PNG, correct content-type, 404 on invalid/path-traversal names) and confirmed all 3 thumbnails render with real image dimensions in an actual browser. See `.pHive/epics/hdl-icon-reconciliation/docs/design-discussion.md`. Version bump `0.31.0` → `0.32.0`.

- **`hdl-policy-path-fix`: the desktop app's real `.app` bundle couldn't find `config/routing-policy.yaml`.** Found by actually installing the app for real dogfooding (per operator request) and re-verifying live after a rebuild — `GET /routing-policy` returned `503`, `ENOENT` against the app-data directory. `PolicyLoader`'s default path resolves via `process.cwd()`, which `hdl-desktop-app` deliberately redirects to a per-user app-data directory (for `.env`/DB persistence) — two independently-correct pieces of work combining into a real defect. Not just a dashboard bug: `ScoredStrategy` calls `PolicyLoader.load()` the same way with no override, so this affected real routing-strategy correctness too. Fixed by extending `HEIMDALL_REPO_ROOT` (the same override `hdl-docs-viewer` already established for this exact class of problem) to the policy loader's default path, and by staging `config/` into the bundled resource directory in `build-resources.sh` (it was never copied at all). The path-resolution logic was factored into a small pure `resolveDefaultPolicyPath()` function specifically so this class of bug is unit-testable going forward. Reinstalled the real `.app` and confirmed live. See `.pHive/epics/hdl-policy-path-fix/docs/design-discussion.md`. Version bump `0.27.0` → `0.28.0`.

### Added

- **`hdl-routing-policy-dashboard` epic complete.** `docs/vision.md` named this explicitly under both "Goals" and "Good first contributions": the scored strategy's `config/routing-policy.yaml` is hand-edited with no dashboard visibility. New `GET /routing-policy` wraps the existing (already fully validated) `PolicyLoader.load()` read-only, loaded fresh on every request so a hand-edit is reflected immediately without a restart. Dashboard gets a "Routing policy" panel — per-task-type provider weights, headroom floor, cost preference, experiment arm status — following the same pattern as the existing Telemetry/Model catalog panels. Live-verified in a real browser via Playwright (`page.evaluate()` confirming the rendered DOM), not just curl. Picked up autonomously after a backlog scan found no open GitHub issues and no genuine design fork for this item (the two remaining backlog items — headroom/cost-tier auto-inference, further probe-cadence backoff — do need operator input and were left alone). See `.pHive/epics/hdl-routing-policy-dashboard/docs/design-discussion.md`. Version bump `0.26.0` → `0.27.0`.

- **`hdl-desktop-app` epic complete — standalone Tauri v2 desktop app, phase 2 of standalone-app packaging.** Per operator direction: *"just like portunus and the rest, let's get this into a standalone app, test it out and even give it an install and setup to go on an single point of install by itself so we can see and navigate the docs and work with the diagrams etc -- once i can do that, we are fully dogfooding this and i can lock it down."* Adapted directly from Portunus's own real, shipped Tauri v2 desktop app — the only reference implementation in the Pantheon ecosystem (confirmed no shared cross-god template exists).
  - A Rust shell (new top-level `app/` directory) spawns Heimdall's compiled `dist/src/main.js` as a sidecar subprocess: captures the user's real login-shell PATH (a GUI-launched macOS process gets a near-empty one — a confirmed, not hypothetical, gotcha), binds an OS-assigned free port, and points `cwd`/`HEIMDALL_DB_PATH`/`HEIMDALL_REPO_ROOT` at a stable per-user app-data directory rather than wherever the `.app` bundle happens to be installed. Polls `/healthz` before the webview navigates off the loading placeholder.
  - Tray icon (Open/Check-for-Updates/Launch-at-Login/Quit) with close-to-tray behavior; self-updates by shelling out to the user's own already-authenticated `gh` CLI (never an embedded token) with a native confirm dialog and a detached relauncher script (verify-before-touch + backup-and-restore-on-failure). Ad-hoc code signing only — single-machine target, no Apple Developer Program distribution.
  - `build-resources.sh` is a genuine rewrite, not an adaptation: unlike Portunus's self-bundling Next.js `standalone` output, Heimdall's plain `tsc` build does not include its own npm dependencies, so production deps are staged via a clean `npm ci --omit=dev` inside the staging directory — never touching the live checkout's own `node_modules`.
  - **Live-verified end-to-end, not just compiled**: a real `cargo tauri dev` launch spawned the sidecar (confirmed via `ps`/`lsof`), passed its health check, and the actual native window rendered the live dashboard (screenshotted — not the loading placeholder). `/lanes`, `/docs`, `/docs/architecture`, and `/vendor/mermaid.min.js` all served correctly through the packaged sidecar; the SQLite DB persisted at the correct macOS `Application Support` path.
  - **Found and fixed a real bug during that verification**: Cmd+Q delivered `RunEvent::Exit` directly on this build, with no preceding `ExitRequested` — the event the first draft (matching Portunus's own pattern) relied on exclusively, leaving the sidecar `node` process orphaned after quit. Confirmed via a real Cmd+Q keypress and `ps`, not assumed. Fixed by handling cleanup on both `RunEvent` variants (naturally idempotent). Re-verified live: both processes now exit cleanly.
  - **Same-day fast-follow**: the one gap noted at merge time — the actual `cargo tauri build` release bundle, not just `cargo tauri dev` — is now closed. Built a real ad-hoc-signed `Heimdall.app`, installed it to `/Applications`, and launched it exactly as a user double-clicking it would: sidecar spawned, `/healthz`/`/docs` served correctly, the installed app's window rendered the live dashboard (screenshotted), and Cmd+Q cleanly exited both the app and the sidecar. Confirms the release-mode `resource_dir()` path works, not just the dev-mode repo-checkout fallback.
  - See `.pHive/epics/hdl-desktop-app/docs/design-discussion.md`. Version bump `0.25.0` → `0.26.0`.

- **`hdl-docs-viewer` epic complete — phase 1 of standalone-app packaging.** "See and navigate the docs and work with the diagrams" from inside the running service itself. New `GET /docs` (index) and `GET /docs/:slug` (rendered page), served on the same HTTP server the dashboard already runs on. Markdown rendered server-side (`marked`), Mermaid diagrams rendered client-side against a locally-vendored `mermaid.min.js` (served from `node_modules` directly — no CDN, no network call, same principle the dashboard already holds to). Doc list is a fixed, explicit manifest, not a live directory scan — `:slug` validates against a known-safe allowlist rather than resolving an arbitrary path off disk. Researched Portunus's own real desktop-app packaging first and confirmed it has no in-app docs/diagram viewer at all (its "About" tab is hand-authored static prose) — this is genuinely new work. Verified live in a real browser: the actual Mermaid architecture diagram renders correctly. See `.pHive/epics/hdl-docs-viewer/docs/design-discussion.md`. Version bump `0.24.0` → `0.25.0`.

- **`hdl-error-taxonomy` epic complete — full error codes and responses, across every provider.** Generalizes the prior `hdl-429-corroboration` fix (Claude-only) into a real cross-provider architecture, per operator direction: *"we do not just show our status of up, down, intermittent or whatever, we have FULL error codes and responses and give it back... we have to internally know the action to take and OUR state could be one of the 3 but then with full details underneath."*
  - New normalized `ErrorCode` (`rate_limit | quota_exceeded | billing_error | auth_failed | server_error | network_error | unknown`) alongside the existing free-text `reason` — never one instead of the other. `LaneStatus`/`GET /lanes`/the dashboard all surface it; `LaneStatusValue` (the 4-state action model) is unchanged.
  - **Real audit + real research found and fixed actual bugs, not just gaps**: Codex and Kimi were both passing the raw `retry-after` header straight through as `reset_at` (`Date.parse("30")` → `NaN`, silently discarded by the scheduler — the real retry timer was never actually used). Both now convert it to a real absolute timestamp. `lane-pipeline.ts` was discarding a real `reset_at`/error detail on the *first* (uncorroborated) signal tick, re-detecting it a full cycle later — now preserved immediately as diagnostic detail while the *status* itself still stays conservative until corroborated.
  - Every provider's active-probe adapter (Claude, Codex, Gemini, Kimi, OpenRouter, Ollama) now maps its real error condition to a normalized code, researched against each provider's actual official docs rather than guessed — including an honest `null` for OpenRouter's `X-RateLimit-Reset`, whose format is genuinely undocumented (confirmed, not assumed) rather than a confidently-wrong guess.
  - **Timers now drive real scheduling action, not just display**: `InProcessScheduler` backs `auth_failed` lanes off to a fixed 5-minute recheck instead of the flat ~5s cadence — safe specifically because auth failures can't self-heal by retrying (only an operator fixing the credential does), so there's no self-healing event within the 10-second SLA window to risk missing. Every other error class keeps the existing SLA-driven fine cadence unchanged. An operator's explicit `manual_reset_at` still wins outright over this automatic backoff.
  - See `.pHive/epics/hdl-error-taxonomy/docs/design-discussion.md`. Version bump `0.23.0` → `0.24.0`.

- **`hdl-429-corroboration` resolved.** A long-open decision (`docs/decisions/DEC-hdl-429-corroboration.md`, parked 2026-08-12) about how a Claude 429 should resolve. Per operator direction — keep the immediate, uncorroborated verdict, but stop discarding the real diagnostic content Anthropic actually sends ("we had to have multiple statuses AND reason and the 429 usually has a reason with it"). Real research against Anthropic's official API docs confirmed a 429 carries far more than a bare status: a real `error.message` body plus several `anthropic-ratelimit-*` headers and `retry-after`, none of which Heimdall was reading. Now: the real message becomes `reason` (via the same `parseClaudeCapSignal()` extraction `rotation-controller.ts` already uses — one implementation, not two), and a message naming a **weekly limit** resolves to `out_of_credit` instead of `degraded` — the "multiple statuses" half of the fix, since a weekly cap is the same severity class as billing `out_of_credit`, not a transient rate limit. `reset_at` still prefers the specific `anthropic-ratelimit-requests-reset` header, falling back to `retry-after`. Codex's equivalent handling is untouched, flagged as a separate follow-up if wanted. Version bump `0.22.0` → `0.23.0`.

- **`hdl-route-outcome-feedback` epic complete.** `RouteLedger.reportOutcome()` existed and was tested but nothing called it — `docs/vision.md` named this explicitly: "callers need an outcome-feedback surface." A caller that got a `decision_id` from `POST /route` (or CLI `route`/MCP `route_selection`) can now report back what happened. `POST /route/:decisionId/outcome` (body `{outcome?, actual_cost?, metadata?}`), CLI `route-outcome --decision-id=<id> [--outcome=<value>] [--actual-cost=<n>]`, and MCP `heimdall.route.reportOutcome` — one shared `reportRouteOutcome()` function, three thin transports, matching every prior mutation surface in this codebase. 404 `unknown_decision` for an unrecognized id, never a crash. See `.pHive/epics/hdl-route-outcome-feedback/docs/design-discussion.md`. Version bump `0.21.0` → `0.22.0`.

- **`hdl-own-telemetry` epic complete.** Heimdall gets its own self-contained metric tracking, independent of Argus. Operator directive (2026-08-14), in response to a proposal to add Argus OTEL emission for routing decisions: *"heimdall should have it's own telemetry and stuff, we need to be working completely in a silo... each god needs its own metric tracking done separately -- argus is just another silly dashboard for otel alike posthog or anything else."* Audit found actuation results, rotation events, and model substitutions had zero local record — Argus-only fire-and-forget OTEL push, gone entirely if Argus wasn't configured (the common standalone-mode case).
  - New `telemetry_events` StateStore table for the three event kinds with no existing local record. `LocalTelemetryRecorder` implements the same 3-method `ArgusEmitter` interface `ArgusClient` does, writing locally instead of pushing OTEL. `CompositeTelemetryEmitter` fans one call out to a list of emitters — the single construction-site change in `composeService()` (`new ArgusClient()` → a composite of `[LocalTelemetryRecorder(store), ArgusClient]`) means every existing call site keeps its `ArgusEmitter`-typed parameter completely unchanged. Argus keeps receiving the exact same spans it always has; Heimdall just stops depending on it.
  - `rotation-controller.ts`/`route-selector.ts` now record rotation events and model substitutions locally — genuinely new instrumentation, since neither ever emitted telemetry of any kind before, not even to Argus.
  - New `GET /metrics` — hand-rolled Prometheus text exposition format, no new dependency. Aggregates lane counts, actuation results, rotation events, model substitutions, routing decisions, and model-catalog entries, all from Heimdall's own local state. Scrapable by Argus, Grafana, Prometheus, or anything else OTEL/Prometheus-compatible later, without Heimdall depending on any of them being present.
  - Dashboard gets a "Telemetry" panel parsing `GET /metrics` client-side.
  - See `.pHive/epics/hdl-own-telemetry/docs/design-discussion.md`. Version bump `0.20.0` → `0.21.0`.

## [0.20.0] - 2026-08-14

First tagged release. Everything below accumulated in `[Unreleased]` prior to this
cut — each entry documents its own version bump inline, so this section is left as a
single chronological narrative rather than split into synthetic per-bump releases.

### Added

- **`hdl-routing-reconciliation` epic complete — dev/main branch reconciliation.** This whole `[Unreleased]` section's epics (through `hdl-model-catalog-dashboard`) were mistakenly built on `main` instead of `dev`, the project's actual convention (feature branches off `dev`, merged back to `dev`, `main` only moves on a `dev`-cut release). Meanwhile `dev` independently shipped an advisory routing layer — weighted candidate scoring, deterministic A/B experiment arms, a decision ledger, and multi-account rotation with cap-aware failover — never merged forward, discovered via a full investigation before any merge action was taken. Per operator decision (Claude Artifact, Option B): main's pluggable `routing-strategies/` interface stays canonical rather than being replaced by dev's scorer.
  - **Interface refactor**: `RoutingStrategy.selectRoute(taskType, candidates): Lane | null` widened to `selectRoute(ctx: RouteSelectionContext): RouteSelectionResult` (`ctx = {taskType, candidates, store, taskId?}`, `result = {lane, detail?}`) — `priority`/`round-robin`/`off` changed signature only, zero behavior change, proven by the full pre-existing test suite passing with identical assertions.
  - **New `scored` strategy**: dev's `scorer.ts`/`policy-loader.ts`/`experiment-assigner.ts`/`rationale-generator.ts`/`route-ledger.ts` ported unchanged, wrapped in a new strategy implementing the widened interface — selectable via the existing `GET`/`POST /routing-strategy`, exactly like every other strategy. `GET /available-route` surfaces `rationale`/`decision_id`/`experiment_arm`/`ranked_candidates` when `scored` is active, absent otherwise. `POST /route` (and CLI `route`, MCP `route_selection`) always use `scored` regardless of the globally active strategy, preserving Auriga/Minerva's existing dispatch contract. `HEIMDALL_LANE_N_HEADROOM`/`_COST_TIER` lane fields ported from dev (defaults `10000`/`medium`).
  - **`rotation-controller.ts` + `error-parser.ts` wired live for the first time on either branch**: both existed, fully unit-tested, but were never instantiated in `composeService()`. Now: one `RotationController` per provider with 2+ credentialed lanes (never for a single-lane provider — nothing to rotate between), the cap-reset background job, and `GET`/`POST /rotation/:provider[/rotate]` (never returns the resolved credential). Wrapping the live Claude completion call through `RotationController.request()` is an explicit, documented follow-up — not done here.
  - **`token-registry.ts` explicitly NOT ported** — confirmed orphaned on `dev` (unwired, nothing imports it outside its own test), superseded by the `credential_ref` model already shipped.
  - Every other capability from both branches folds forward as-is: 6-provider signal adapters, model-catalog trilogy, dashboard, lane-management (main); `org-tree`, `argus`/`minerva` adapters, bot handlers, OSS scaffolding (dev). See `.pHive/epics/hdl-routing-reconciliation/docs/design-discussion.md`. Version bump `0.19.0` → `0.20.0`.

- **`hdl-model-catalog-dashboard` epic complete (slice 2 of 3 — the model-catalog trilogy is now fully shipped).** The dashboard gets a "Model catalog" panel — grouped by provider, per-model enable/disable checkboxes, and a Refresh button that pulls each configured provider's live model list on demand. Pure UI on top of the `GET`/`POST /models` surface `hdl-model-catalog` already shipped; no backend changes. Loaded once on page load, not on the 5s lane-status poll — mirrors the routing-strategy panel's exact reasoning (a config-browsing surface, not a live status row). Verified live in a real browser: Refresh correctly round-trips through the real endpoint, banner updates, empty state renders correctly with no models configured yet. See `.pHive/epics/hdl-model-catalog-dashboard/docs/design-discussion.md`. Version bump `0.18.0` → `0.19.0`.

- **`hdl-model-catalog-routing` epic complete (slice 3 of 3 — the actual fix).** Closes the model-catalog trilogy: `GET /available-route` now returns a model that's confirmed usable, not just whatever was declared. When a lane's `HEIMDALL_LANE_N_MODEL` is disabled (operator choice, or defaulted-off as an older generation) or has vanished from the live catalog entirely, Heimdall substitutes the most recently-created **enabled** model for that provider instead of handing back a value the provider will reject — directly closing the operator's original complaint about agents calling deprecated Gemini 2.5 models. Never silent: `AvailableRoute` gains `model_substituted: boolean`. Byte-identical fallback when no catalog data exists yet for a provider ("no data, no opinion") — confirmed live against the real running service. Dashboard toggles (slice 2) remain open/optional. See `.pHive/epics/hdl-model-catalog-routing/docs/design-discussion.md`. Version bump `0.17.0` → `0.18.0`.

- **`hdl-model-catalog` epic complete (slice 1 of 3).** Heimdall now answers "what models can I actually call right now" — closing the operator's complaint about agents calling deprecated Gemini 2.5 models. Research found no provider API exposes deprecation status directly; an initial git-tracked-catalog-file design was corrected by the operator (Heimdall is OSS, installed independently by many operators with their own real model access — the catalog must be live-fetched per installation, not shipped in a release). Each configured lane's provider's live model list is fetched using that installation's own credentials (`signal-sources/model-list/`, reusing the exact URL/auth every active-probe adapter already uses), stored locally in `StateStore` (same gitignored, per-installation home as `manual_override`/`routing_strategy`), with a per-provider "newest models" default-enable heuristic applied only the first time a model is seen (`created`-timestamp sort for Claude/Codex/Kimi, a generation-number heuristic for Gemini since it has no date field at all) — fully operator-overridable, and an operator's explicit choice is guaranteed to survive every future refresh. New `GET`/`POST /models` routes and `heimdall.models.list`/`.refresh`/`.setEnabled` MCP tools expose it. Confirmed live: a Claude Code subscription-token lane correctly (and gracefully) returns an empty catalog, since Anthropic's direct `/v1/models` endpoint rejects that credential type the same way it rejects completions — a known, documented gap, not a bug. Dashboard toggles and auto-substituting route selection when a model disappears are deferred to slices 2/3. See `.pHive/epics/hdl-model-catalog/docs/research-brief.md`. Version bump `0.16.0` → `0.17.0`.

- **`hdl-claude-subscription-lanes` epic complete.** `provider: "claude"` lanes now support two credential shapes, auto-detected — a raw Anthropic API key (unchanged) and a Claude Code long-lived OAuth token (`sk-ant-oat01-...`, from `claude setup-token`), closing a real gap between the north star's "2-5 Claude Code subscriptions" framing and the API-key-only adapter shipped since `lane-health-status`. Confirmed via research that Anthropic's Messages API rejects this token type directly outside the genuine Claude Code client, so the correct integration path shells out to the real `claude` CLI, reusing the `CommandRunner` pattern already established for Multica (`CommandRunner.run()` gains an additive, backward-compatible env-override option). **A live adversarial test caught a real design flaw before it shipped**: the originally-planned `claude auth status --json` check reported a fabricated token as valid even in a fully isolated environment — it only inspects a token's shape, never validates it against Anthropic's servers. Replaced with a real minimal completion call (`claude -p ... --max-turns 1`), verified correct in both directions against a fabricated token and a genuine one — the one active-probe in the codebase that spends real, non-zero inference, since there's no free way to validate this credential type. See `.pHive/epics/hdl-claude-subscription-lanes/docs/research-brief.md`. Version bump `0.15.0` → `0.16.0`.

- **`hdl-mcp-routing-strategy` epic complete.** Closes the follow-up explicitly deferred when `hdl-routing-strategies` shipped: two new MCP tools, `heimdall.routingStrategy.get` (returns `{ active, available }`, identical shape to `GET /routing-strategy`) and `heimdall.routingStrategy.set` (`{ strategy }`, wraps the existing `setRoutingStrategy` shared function — invalid names return structured `{ error: "invalid_strategy", allowed_strategies }` content rather than throwing). Zero new business logic — both tools call the exact same shared functions the HTTP routes already use. Version bump `0.14.0` → `0.15.0`.

- **`hdl-ollama-signals` epic complete — closes the north star's provider list to 6/6.** Ollama gets a liveness-only signal adapter, per operator decision ("liveliness it doesn't really have a path -- it should have liveliness so we can signal off of it mostly") — only `up`/`down`, no `degraded`/`out_of_credit`/`reset_at`, since none of those have a real signal for local unauthenticated inference. `active-probe/ollama.ts` hits `GET <baseUrl>/api/tags` with no auth. Implementation research found a cleaner path than originally anticipated: rather than making `Lane.credential` optional (touching `lane-registry.ts`/`route-selector.ts`/`lane-pipeline.ts`'s credential-gating logic), the existing `credential_ref` indirection is repurposed to carry the Ollama instance's base URL instead of a secret (e.g. `HEIMDALL_LANE_<N>_CREDENTIAL_REF=OLLAMA_HOST`, `OLLAMA_HOST=http://localhost:11434`) — this flows through the completely unmodified pipeline, and as a side benefit supports multiple Ollama instances on different hosts (north_star's "scales... as hardware/instances are added") with zero new declaration fields. Wired via the usual `ollamaAdapters()` + `PROVIDER_ADAPTERS` entry. See `.pHive/epics/hdl-ollama-signals/docs/design-discussion.md`. Version bump `0.13.0` → `0.14.0`.

- **`hdl-openrouter-signals` epic complete.** OpenRouter now nests as a gateway with independently toggleable, judged routes underneath it, per operator decision — not one more flat lane. A dedicated codebase research pass confirmed the persistence layers needed zero schema changes (`lane_id` is the only identity key anywhere; multiple lanes already shared one `credential_ref` today). Closed the two real gaps found: `active-probe/openrouter.ts` (`GET /api/v1/key` — a single call returning both credit balance and rate-limit state, richer than a bare list-models probe; no `public-status/openrouter.ts` since no confirmed machine-readable feed exists) wired via the usual `openrouterAdapters()` + `PROVIDER_ADAPTERS` entry, proven end-to-end with two lanes sharing one credential and independently recording status/override; the dashboard now shows each lane's `model` and groups lanes sharing a gateway `credential_ref` under one header (`GET /lanes` gains `model`/`credential_ref`, both non-secret metadata already on the in-memory `Lane`); and an optional `HEIMDALL_LANE_<N>_PRIORITY` override lets the operator rank same-provider routes directly (`priority-strategy.ts`'s table-driven rank was previously blind to same-provider ties) — unset is byte-identical to prior behavior, mirroring `manual_override`'s override-wins-outright shape. See `.pHive/epics/hdl-openrouter-signals/docs/design-discussion.md`. Version bump `0.12.0` → `0.13.0`.

- **`hdl-kimi-signals` epic complete.** Kimi K3 (Moonshot AI) is now a fourth supported lane provider — 4 of 6 north-star providers now have signal adapters. `active-probe/kimi.ts` mirrors `codex.ts`'s Bearer-auth + OpenAI-API-compatible-host pattern (`GET api.moonshot.ai/v1/models`), reading a typed `error.type` vocabulary that distinguishes transient conditions (`engine_overloaded`, `too_many_requests` → `degraded`) from longer-lived quota exhaustion (`billing_quota_exhausted`, `monthly_quota_exhausted`, `rolling_quota_exceeded` → `out_of_credit`) — richer than Codex's own undocumented guesswork. `public-status/kimi.ts` is a near-verbatim mirror of `claude.ts`: `status.moonshot.cn` is a real Atlassian-Statuspage feed (confirmed via its `api/v2/summary.json` endpoint), a better fit than Gemini's incidents.json workaround. Wiring was the same 3-line addition established by the last two provider epics (`kimiAdapters()` + one `PROVIDER_ADAPTERS` entry). See `.pHive/epics/hdl-kimi-signals/docs/design-discussion.md`. Version bump `0.11.0` → `0.12.0`.

- **`hdl-gemini-signals` epic complete.** Gemini is now a third supported lane provider — closes the largest concrete gap against the north star's provider-scale target (`Claude, Codex, Gemini, OpenRouter, Kimi K3, Ollama`; only 2 of 6 had signal adapters before this epic). Real web research into Gemini's actual signal surface informed two new adapters mirroring the existing `claude.ts`/`codex.ts` pattern exactly: `active-probe/gemini.ts` (`GET generativelanguage.googleapis.com/v1beta/models`, authenticated via the `x-goog-api-key` header rather than the leakier `?key=` query param; a 429 body's `error.status`/`message` distinguishes a transient rate limit — `degraded` — from a longer-lived daily quota condition — `out_of_credit`) and `public-status/gemini.ts` (Google Cloud's `status.cloud.google.com/incidents.json` feed, fragment-filtered to Gemini/Vertex-AI-related products since — unlike Claude's fixed per-component snapshot — this is a flat, Google-Cloud-wide incident list with no positive "operational" entry to read). One confirmed, documented gap versus Claude: the Gemini Developer API has no reset-timestamp field for 429s, so `reset_at` is always `null` for Gemini probes — `InProcessScheduler`'s existing reset_at-unknown fallback (`hdl-reason-aware-recovery`) already handles this gracefully. Wiring was a 3-line addition to already-provider-agnostic code (`geminiAdapters()` in `lane-pipeline.ts`, one `PROVIDER_ADAPTERS` entry in `main.ts`) — `Lane`/`LaneDeclaration`, `LanePipeline`, route selection, and every HTTP/MCP/UI surface needed zero changes, confirming the `ProviderSignalAdapter` pattern actually generalizes. See `.pHive/epics/hdl-gemini-signals/docs/design-discussion.md`. Version bump `0.10.0` → `0.11.0`.

- **`lane-health-status` epic complete (v1/P0).** Health/status detection for Claude and Codex lanes across a layered signal model (passive observation, public status-page piggybacking, sparse active probes), a corroboration policy guarding against provider false-positives, and the `LaneRouterContract` — a synchronous request/response query surface exposed identically over HTTP (`GET /lanes`), CLI (`npm run cli`), and MCP (`heimdall.lanes.list`). SLA-verified: status correctness within 10 seconds of an actual state change (measured, not asserted — see `test/sla-harness/report.md`). `/execute` applied the planned `minor` version bump (`0.1.0` → `0.2.0`) to keep the package version in lockstep with this release.
- **`hdl-scheduler` epic complete.** Pluggable per-lane `Scheduler` interface with two HARD-LAW-compliant backends — `MulticaAutopilotScheduler` (default, coarse cron via the real Multica CLI, ≥1min floor) and `InProcessScheduler` (fine ~5s, suspect-lane only, backs off immediately on recovery). Every tick and status flip emits OTEL to Argus (the first Node/TypeScript OTLP client in Pantheon). Added `POST /lanes/:laneId/refresh` (the trigger endpoint Multica's dispatched agent calls) and a stubbed third interaction mode (`ActuationStub`, future Multica runtime on/off toggling). `src/main.ts` is now the real service entrypoint. See `docs/decisions/DEC-hdl-scheduler-backend.md`. `/execute` applied the planned `minor` version bump (`0.2.0` → `0.3.0`).
- **`hdl-actuation` epic complete (v2).** Heimdall now actually actuates, not just senses: `MulticaControlAdapter` calls Multica's real REST API to disable/re-enable a lane's mapped agents (`max_concurrent_tasks: 0`/`N` — the verified-safe lever, corrected from the original design doc's wrong `status`/`archive` proposal) via a flaky-connection-hardened `MulticaRestClient` (timeout + never-throws `MulticaCallResult`) wrapped in a generic `CircuitBreaker`. Lane → agent mapping is explicit config (`HEIMDALL_LANE_<N>_MULTICA_AGENT_IDS`) behind a `LaneAgentResolver` interface. Every lane always gets a `ControlAdapter` — mapped lanes get the real `MulticaControlAdapter`, everything else (including the whole service when Multica isn't configured at all) falls back to a loud `StubControlAdapter`, never a silent no-op. `reconcile()` runs every sense-loop tick for every lane, giving idempotent retry-for-free on partial failures, with every attempt emitted to Argus (`emitActuationResult`). Tested entirely against local mocks — the real hive Multica instance (`:8090`) is never called by this test suite; live end-to-end verification is an explicit operator follow-up. See `docs/decisions/DEC-hdl-role-actuation.md`. `/execute` applied the planned `minor` version bump (`0.3.0` → `0.4.0`).
- **`hdl-reason-aware-recovery` epic complete.** Lane recovery now consumes the `reason`/`reset_at` data Heimdall already captures, instead of treating status as the only signal. `InProcessScheduler` schedules a suspect lane's next probe at its known `reset_at` (e.g. an out-of-credit reset timestamp) instead of blindly polling every 5s, floored at the existing interval so a past/stale `reset_at` can never busy-loop; falls back to the existing flat interval when `reset_at` is unknown. `ControlAdapter.reconcile` now optionally carries `reason`/`reset_at` alongside status, so `MulticaControlAdapter`'s Argus emission (`lane.reason`/`lane.reset_at` span attributes) and `StubControlAdapter`'s recorded actions can say *why* a lane is blocked, not just that it is — out-of-credit-until-reset_at and down-for-unknown-reason are now distinguishable in telemetry. The block/allow actuation decision itself is unchanged. See `docs/decisions/DEC-hdl-reason-aware-recovery.md`. Version bump `0.4.0` → `0.5.0`.
- **`hdl-lane-status-ui` epic complete (slice 1).** First vertical slice of Heimdall's standalone-mode UI requirement (`has_ui: true`): `GET /` now serves a self-contained, dependency-free read-only dashboard (inline HTML/CSS/JS, no build step, no framework) that polls the existing `GET /lanes` endpoint every 5s and renders lane status with a color-coded badge, reason, and a human-formatted `reset_at`. Pure consumer of already-shipped data — no new backend query logic. Manual disable/enable, add-lane, and the MCP agent-tooling surface for the same operations are explicitly deferred to follow-up epics. See `.pHive/epics/hdl-lane-status-ui/docs/design-discussion.md`. Version bump `0.5.0` → `0.6.0`.
- **`hdl-lane-override` epic complete.** Manual lane disable/enable, routed through the *existing* `ControlAdapter.reconcile()` decision rather than a separate mechanism — a per-lane `manual_override` (`enabled`/`disabled`/`null`) wins outright over the sensed status when set (byte-identical behavior when unset), exposed via `POST /lanes/:laneId/override` (`state: enabled|disabled|auto`) and surfaced on `GET /lanes`. `MulticaControlAdapter`'s Argus emission gains `overrideActive` so an override-driven block/allow is distinguishable from a status-driven one in telemetry. The dashboard (`hdl-lane-status-ui`) gets per-lane Enable/Disable/Auto controls plus a "manual: …" badge that's always visible when an override is active — never silent. See `docs/decisions/DEC-hdl-reason-aware-recovery.md` item 3 and `.pHive/epics/hdl-lane-override/docs/design-discussion.md`. Version bump `0.6.0` → `0.7.0`.
- **`hdl-lane-management` epic complete.** Closes out the remaining UI scope of `has_ui: true`: (1) `.env` is now auto-loaded (`--env-file-if-exists=.env`, zero new dependency) — fixes a standing gap where the operator had to manually source it; (2) `POST /lanes` adds a new lane, writing a `HEIMDALL_LANE_<N>_*` block to the local, gitignored `.env` (Portunus remains the deferred fix for real secret storage) and naming the exact restart command, no automatic restart; (3) `GET /lanes` gains `credential_configured: boolean` — visibility into token status without ever serializing the raw secret; (4) manual `reset_at` ("change the times") mirrors `manual_override`'s pattern exactly — an operator-set recovery time wins over the sensed one in `InProcessScheduler`'s already-shipped reset_at-aware delay computation, scheduling-only. The dashboard gets an Add Lane form, a token-configured chip, and an editable reset-at control with its own "manual" badge. See `docs/decisions/DEC-hdl-reason-aware-recovery.md` item 3 and `.pHive/epics/hdl-lane-management/docs/design-discussion.md`. Version bump `0.7.0` → `0.8.0`.
- **`hdl-mcp-lane-tools` epic complete — closes `DEC-hdl-reason-aware-recovery.md` item 3 in full.** `override`/`reset-at`/`add-lane`'s HTTP-route validation logic is extracted into shared functions (`setLaneOverride`/`setLaneResetAt`/`addLane` in `src/api/http-server.ts`), mirroring `getLaneStatuses`'s existing shared-across-surfaces role — a behavior-preserving refactor verified by the full pre-existing HTTP test suite passing with zero test changes. Three new MCP tools (`heimdall.lanes.override`, `heimdall.lanes.setResetAt`, `heimdall.lanes.add`) wrap those functions alongside the original `heimdall.lanes.list`, so an agent can exercise/test every lane operation Heimdall supports, not just query status. Validation failures return structured content rather than throwing — only a genuinely unrecognized tool name produces a real MCP protocol error. See `.pHive/epics/hdl-mcp-lane-tools/docs/design-discussion.md`. Version bump `0.8.0` → `0.9.0`.
- **`hdl-routing-strategies` epic complete.** Resolves the north_star's own `avoid` clause ("don't hard-code routing heuristics ... must be A/B-testable/reconfigurable"): route selection is now pluggable behind a `RoutingStrategy` interface (`src/core/routing-strategies/`) — `priority` (today's original ranking, still the default), `round-robin` (cycles through eligible lanes), and `off` (never picks — the caller decides via `GET /lanes` instead). Selected via a new global settings table on `StateStore` and `GET`/`POST /routing-strategy`. Also fixes a real gap found along the way: `manual_override` didn't previously affect `/available-route` at all — a disabled lane could still be recommended. It now gates candidacy with the exact same override-wins-outright precedence `MulticaControlAdapter` already uses, so "block the usage and direct it around" actually works end-to-end. Dashboard gets a simple settings panel to pick the active strategy, with the `off` state's consequence always visibly flagged. See `.pHive/epics/hdl-routing-strategies/docs/design-discussion.md`. Version bump `0.9.0` → `0.10.0`.
